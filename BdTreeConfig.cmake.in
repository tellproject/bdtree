set(_comp_file "${CMAKE_CURRENT_LIST_DIR}/BdTreeTargets.cmake")
if(TARGET bdtree)
    set(BdTree_FOUND True)
elseif(EXISTS ${_comp_file})
    include(${_comp_file})
    set(BdTree_FOUND True)
else()
    set(BdTree_FOUND False)
endif()
